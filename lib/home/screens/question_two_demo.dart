import 'package:flutter/material.dart';

class QuestionTwoDemo extends StatelessWidget {
  static Route route() {
    return MaterialPageRoute<void>(builder: (_) => QuestionTwoDemo());
  }

  const QuestionTwoDemo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
